name: Release
on: { push: { tags: [ 'v*' ] } }
concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: true
jobs:
  build-container:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: write
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Build container
        uses: infrabits/ci-pack@b333d34aacd3b2588fc9a1c5fbf22b76728a4cc7 # main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          image: ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          use_builder: latest
          buildpack: https://github.com/InfraBits/cmake-buildpack/releases/download/v1.0.1/buildpack.tar.gz

  create-release:
    runs-on: ubuntu-latest
    needs: [build-container]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Extract artifacts
        run: |
          mkdir -p release/{bin,data}
          docker pull ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          cid=$(docker create ghcr.io/${{ github.repository }}:${{ github.ref_name }})

          # Binaries
          for obj in cluebotng create_ann create_bayes_db print_bayes_db;
          do
            docker cp "${cid}:/workspace/${obj}" release/bin/${obj}
          done

          # Databases
          for obj in main_ann.fann bayes.db two_bayes.db;
          do
            docker cp "${cid}:/workspace/data/${obj}" release/data/${obj}
          done

          docker rm "${cid}"

          # Config files
          tar -czf release/conf.tar.gz conf

      - name: Create GitHub release
        uses: infrabits/ci-create-missing-release@28473a12792785aeaf0ad393cef6664653d32e61 # main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
          artifacts: |
            release/data/**
            release/bin/**
            release/conf.tar.gz
